package com.linksfoundation.dq.core.converter.standard;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * This is the main class for the StandardConverterApplication.
 * The main() method uses Spring Boot's SpringApplication.run() method to launch an application.
*/
@SpringBootApplication
public class StandardConverterApplication {
    public static void main(String[] args) {
        SpringApplication.run(StandardConverterApplication.class, args);}
}